import { PortfolioTemplate } from '../../components/templates/portifolio/PortfolioTemplate'
import './Portfolio.scss'

export const Portfolio = () => {
    return (
        <section id="portfolio" className="portfolio">
            <PortfolioTemplate/>
        </section>
    )
}