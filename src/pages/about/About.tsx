import './About.scss'
import { AboutTemplate } from '../../components/templates/about/About.Template';

export const About = () => {
    return (
        <section id="about" className="about">
            <AboutTemplate/>
        </section>
    )
}