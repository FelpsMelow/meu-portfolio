import './About.scss'
import { AboutTemplate } from '../../components/templates/about/AboutTemplate';

export const About = () => {
    return (
        <section id="about" className="about">
            <AboutTemplate/>
        </section>
    )
}