// import { useTheme } from '../../../hooks/usetheme'
import './AboutTemplate.scss'

export const AboutTemplate = () => {
    // const theme = useTheme()
    return (
        <div className="about-template" style={{background: '#b25cff'}}>
            Template da pagina sobre
        </div>
    )
}